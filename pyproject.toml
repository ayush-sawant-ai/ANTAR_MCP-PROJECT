[project]
name = "webcoder-workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.117.1",
    "mcp[cli]>=1.15.0",
    "uvicorn>=0.37.0",
]
